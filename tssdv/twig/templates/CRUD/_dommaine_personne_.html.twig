
{#  This file is auto-generated and should not be changed by hand.
    filename : _dommaine_personne_.html.twig
    created : Thu, 17 Jun 2021 12:00:12 +0000 UTC
#}

{% extends "CRUD/CRUD.html.twig" %}

{% block main %}


<link rel="stylesheet" href="/{{ App.appName }}/assets/css/forms.css"> 

<div class="wrapper">
    <div id="modal" class="hidden">
        <div id='modal_in'>
            <div class="modal__content">
                <div id='modal_content'>
                    Il faut attendre un peu...
                </div>
                <div class="modal__footer">
                    <button onclick='doDelete2()'>Confirmer la Suppression</button>
                </div>
                <a href="#" class="modal__close" onclick='closeModal()'>&times;</a>
            </div>
        </div>
    </div>
    <form>
        <fieldset>
            <legend><i class='fas fa-history history_entity' data-tipped-options='ajax: {data: { E: "dommaine_personne", id: "{{ dommaine_personne.id }}" }}'></i>dommaine_personne</legend>

{% block actif_full %}
    <p>
    {% block actif_label %}
    <label class='text' for="actif"> actif <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "dommaine_personne", F: "actif", id: "{{dommaine_personne.id}}" }}'></i></label>    
    {% endblock actif_label %}
    {% block actif_active %}
    <input class='text' type="checkbox"  {{ dommaine_personne.actif==1?'checked':'' }} name="actif" id="actif">

    {% endblock actif_active %}
    </p>
{% endblock actif_full %}    

{% block oneToManydommaine_personne_domaine_full %}
<p>
    {% block oneToManydommaine_personne_domaine_label %}
    <label class='select' for='domaine'>
        {{  dommaine_personne.domaine ? dommaine_personne.domaine.getCrudEditLink("domaine")|raw : "domaine"}}
        <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "dommaine_personne", F: "domaine", id: "{{dommaine_personne.id}}" }}'></i>
    </label>
    {% endblock oneToManydommaine_personne_domaine_label %}
    {% block oneToManydommaine_personne_domaine_active %}    
    <input class='text {{ ( dommaine_personne.domaine.id ==0 or  dommaine_personne.domaine.actif)?"":" passif" }}'  value="{{ dommaine_personne.domaine.generic() }}" name="domaine" id="domaine"  _id_="{{ dommaine_personne.domaine.id }}">
    <script>
        setAutocomplete("#domaine", "/tssdv/CRUD/L/Domaine");
    </script>
    {% endblock oneToManydommaine_personne_domaine_active %}    
</p>
{% endblock oneToManydommaine_personne_domaine_full %}

{% block oneToManydommaine_personne_personne_full %}
<p>
    {% block oneToManydommaine_personne_personne_label %}
    <label class='select' for='personne'>
        {{  dommaine_personne.personne ? dommaine_personne.personne.getCrudEditLink("personne")|raw : "personne"}}
        <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "dommaine_personne", F: "personne", id: "{{dommaine_personne.id}}" }}'></i>
    </label>
    {% endblock oneToManydommaine_personne_personne_label %}
    {% block oneToManydommaine_personne_personne_active %}    
    <input class='text {{ ( dommaine_personne.personne.id ==0 or  dommaine_personne.personne.actif)?"":" passif" }}'  value="{{ dommaine_personne.personne.generic() }}" name="personne" id="personne"  _id_="{{ dommaine_personne.personne.id }}">
    <script>
        setAutocomplete("#personne", "/tssdv/CRUD/L/Personne");
    </script>
    {% endblock oneToManydommaine_personne_personne_active %}    
</p>
{% endblock oneToManydommaine_personne_personne_full %}

{% block ordre_full %}
    <p>
    {% block ordre_label %}
    <label class='text' for="ordre"> ordre <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "dommaine_personne", F: "ordre", id: "{{dommaine_personne.id}}" }}'></i></label>    
    {% endblock ordre_label %}
    {% block ordre_active %}
    <input class='text' type="number"  value="{{ dommaine_personne.ordre }}" name="ordre" id="ordre" placeholder="0">

    {% endblock ordre_active %}
    </p>
{% endblock ordre_full %}    

{% block quotite_full %}
    <p>
    {% block quotite_label %}
    <label class='text' for="quotite"> quotite <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "dommaine_personne", F: "quotite", id: "{{dommaine_personne.id}}" }}'></i></label>    
    {% endblock quotite_label %}
    {% block quotite_active %}
    <input class='text' type="number"  value="{{ dommaine_personne.quotite }}" name="quotite" id="quotite" step="any" placeholder="0">

    {% endblock quotite_active %}
    </p>
{% endblock quotite_full %}    

        </fieldset>
        <br>
        <br>
        <br>
        <br>
        </form>
</div>
{% endblock %}       
        