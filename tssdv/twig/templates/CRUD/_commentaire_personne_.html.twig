
{#  This file is auto-generated and should not be changed by hand.
    filename : _commentaire_personne_.html.twig
    created : Wed, 30 Jul 2025 18:30:52 +0200 UTC
#}

{% extends "CRUD/CRUD.html.twig" %}

{% block main %}


<link rel="stylesheet" href="/{{ App.appName }}/assets/css/forms.css"> 

<div class="wrapper">
    <div id="modal" class="hidden">
        <div id='modal_in'>
            <div class="modal__content">
                <div id='modal_content'>
                    Il faut attendre un peu...
                </div>
                <div class="modal__footer">
                    <button onclick='doDelete2()'>Confirmer la Suppression</button>
                </div>
                <a href="#" class="modal__close" onclick='closeModal()'>&times;</a>
            </div>
        </div>
    </div>
    <form>
        <fieldset>
            <legend><i class='fas fa-history history_entity' data-tipped-options='ajax: {data: { E: "commentaire_personne", id: "{{ commentaire_personne.id }}" }}'></i>commentaire_personne</legend>

{% block actif_full %}
    <p>
    {% block actif_label %}
    <label class='text' for="actif"> actif <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "commentaire_personne", F: "actif", id: "{{commentaire_personne.id}}" }}'></i></label>    
    {% endblock actif_label %}
    {% block actif_active %}
    <input class='text' type="checkbox"  {{ commentaire_personne.actif==1?'checked':'' }} name="actif" id="actif">

    {% endblock actif_active %}
    </p>
{% endblock actif_full %}    

{% block oneToManycommentaire_personne_personne_full %}
<p>
    {% block oneToManycommentaire_personne_personne_label %}
    <label class='select' for='personne'>
        {{  commentaire_personne.personne ? commentaire_personne.personne.getCrudEditLink("personne")|raw : "personne"}}
        <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "commentaire_personne", F: "personne", id: "{{commentaire_personne.id}}" }}'></i>
    </label>
    {% endblock oneToManycommentaire_personne_personne_label %}
    {% block oneToManycommentaire_personne_personne_active %}    
    <input class='text {{ ( commentaire_personne.personne.id ==0 or  commentaire_personne.personne.actif)?"":" passif" }}'  value="{{ commentaire_personne.personne.generic() }}" name="personne" id="personne"  _id_="{{ commentaire_personne.personne.id }}">
    <script>
        setAutocomplete("#personne", "/tssdv/CRUD/L/Personne");
    </script>
    {% endblock oneToManycommentaire_personne_personne_active %}    
</p>
{% endblock oneToManycommentaire_personne_personne_full %}

{% block oneToManycommentaire_personne_auteur_full %}
<p>
    {% block oneToManycommentaire_personne_auteur_label %}
    <label class='select' for='auteur'>
        {{  commentaire_personne.auteur ? commentaire_personne.auteur.getCrudEditLink("auteur")|raw : "auteur"}}
        <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "commentaire_personne", F: "auteur", id: "{{commentaire_personne.id}}" }}'></i>
    </label>
    {% endblock oneToManycommentaire_personne_auteur_label %}
    {% block oneToManycommentaire_personne_auteur_active %}    
    <input class='text {{ ( commentaire_personne.auteur.id ==0 or  commentaire_personne.auteur.actif)?"":" passif" }}'  value="{{ commentaire_personne.auteur.generic() }}" name="auteur" id="auteur"  _id_="{{ commentaire_personne.auteur.id }}">
    <script>
        setAutocomplete("#auteur", "/tssdv/CRUD/L/Personne");
    </script>
    {% endblock oneToManycommentaire_personne_auteur_active %}    
</p>
{% endblock oneToManycommentaire_personne_auteur_full %}

{% block date_full %}
    <p>
    {% block date_label %}
    <label class='text' for="date"> date <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "commentaire_personne", F: "date", id: "{{commentaire_personne.id}}" }}'></i></label>    
    {% endblock date_label %}
    {% block date_active %}
    <input class='text' type="datetime-local"  value="{{ commentaire_personne.date|date('Y-m-d') }}T{{ commentaire_personne.date|date('H:i') }}" name="date" id="date">

    {% endblock date_active %}
    </p>
{% endblock date_full %}    

{% block commentaire_full %}
    <p>
    {% block commentaire_label %}
    <label class='text' for="commentaire"> commentaire <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "commentaire_personne", F: "commentaire", id: "{{commentaire_personne.id}}" }}'></i></label>    
    {% endblock commentaire_label %}
    {% block commentaire_active %}
    <textarea mode='raw' class='textarea' name="commentaire" id="commentaire" size="10"  placeholder="">{{ commentaire_personne.commentaire }}</textarea>

    {% endblock commentaire_active %}
    </p>
{% endblock commentaire_full %}    

        </fieldset>
        <br>
        <br>
        <br>
        <br>
        </form>
</div>
{% endblock %}       
        