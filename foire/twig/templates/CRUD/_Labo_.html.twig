
{#  This file is auto-generated and should not be changed by hand.
    filename : _Labo_.html.twig
    created : Mon, 06 Apr 2026 11:00:33 +0200 UTC
#}

{% extends "CRUD/CRUD.html.twig" %}

{% block main %}


<link rel="stylesheet" href="/{{ App.appName }}/assets/css/forms.css"> 

<div class="wrapper">
    <div id="modal" class="hidden">
        <div id='modal_in'>
            <div class="modal__content">
                <div id='modal_content'>
                    Il faut attendre un peu...
                </div>
                <div class="modal__footer">
                    <button onclick='doDelete2()'>Confirmer la Suppression</button>
                </div>
                <a href="#" class="modal__close" onclick='closeModal()'>&times;</a>
            </div>
        </div>
    </div>
    <form>
        <fieldset>
            <legend><i class='fas fa-history history_entity' data-tipped-options='ajax: {data: { E: "Labo", id: "{{ Labo.id }}" }}'></i>Labo</legend>

{% block actif_full %}
    <p>
    {% block actif_label %}
    <label class='text' for="actif"> actif <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "Labo", F: "actif", id: "{{Labo.id}}" }}'></i></label>    
    {% endblock actif_label %}
    {% block actif_active %}
    <input class='text' type="checkbox"  {{ Labo.actif==1?'checked':'' }} name="actif" id="actif">

    {% endblock actif_active %}
    </p>
{% endblock actif_full %}    

{% block nom_full %}
    <p>
    {% block nom_label %}
    <label class='text' for="nom"> nom <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "Labo", F: "nom", id: "{{Labo.id}}" }}'></i></label>    
    {% endblock nom_label %}
    {% block nom_active %}
    <input class='text'  type="text" value="{{ Labo.nom }}" name="nom" id="nom" size="50" maxlength="100", placeholder="">

    {% endblock nom_active %}
    </p>
{% endblock nom_full %}    

{% block acronyme_full %}
    <p>
    {% block acronyme_label %}
    <label class='text' for="acronyme"> acronyme <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "Labo", F: "acronyme", id: "{{Labo.id}}" }}'></i></label>    
    {% endblock acronyme_label %}
    {% block acronyme_active %}
    <input class='text'  type="text" value="{{ Labo.acronyme }}" name="acronyme" id="acronyme" size="50" maxlength="100", placeholder="">

    {% endblock acronyme_active %}
    </p>
{% endblock acronyme_full %}    

{% block url_full %}
    <p>
    {% block url_label %}
    <label class='text' for="url"> url <i class='fas fa-history history_field' data-tipped-options='ajax: {data: { E: "Labo", F: "url", id: "{{Labo.id}}" }}'></i></label>    
    {% endblock url_label %}
    {% block url_active %}
    <input class='text'  type="text" value="{{ Labo.url }}" name="url" id="url" size="50" maxlength="100", placeholder="">

    {% endblock url_active %}
    </p>
{% endblock url_full %}    

    {% block manyToOne_personneList_personneList %} 
    <fieldset>            
        <legend><i class='fas fa-history history_Links' data-tipped-options='ajax: {data: { E: "Labo", id: "{{ Labo.id }}", L: "personneList" }}'></i>personneList - {{ (Labo.personneList)|length }} élément{{(Labo.personneList)|length >1?'s':''}}</legend>

                <ul>
                {% for L in Labo.personneList %}   {% block manyToOne_Labo_labo %}
                    <li class='{{ L.actif?"":"passif" }}'>
                        {{ L.getGeneric(true)|raw }}
                    </li>
                    {% endblock manyToOne_Labo_labo %}
                {% endfor %}
                </ul>
    </fieldset>
    {% endblock manyToOne_personneList_personneList %}    

        </fieldset>
        <br>
        <br>
        <br>
        <br>
        </form>
</div>
{% endblock %}       
        